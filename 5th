# Take user input
day_num = int(input("Enter a number between 1 and 7: "))

# Create a list of day names
days = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday']

# Print the name of the day
if day_num >= 1 and day_num <= 7:
    print("The day is:", days[day_num-1])
else:
    print("Invalid input.")
